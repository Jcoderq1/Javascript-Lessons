let newStr = "";
function capitalize(s, arr) {
  for (const idx of arr) {
    newidx = idx;
  }
  for (let i = 0; i < s.length; i++) {
    if (true) {
      newStr += str.charAt(newidx).toUpperCase();
    } else {
      newStr += str.charAt(newidx);
    }
  }
}
