function capitalize(s, arr) {
  newArray = s.split("");
  newStr = "";
  for (const idx of arr) {
    if (true) {
      let newString = newArray.join("");
      newStr += newString.charAt(idx).toUpperCase();
    } else {
    }
  }
}
capitalize("abcdef", [1, 2, 3]);
