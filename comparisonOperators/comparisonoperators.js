const value = "hello" == "hello";
// the == sign performes a loose equivalence so a number and a string can equal to each other or 1 == true etc..
// the === sign is a strict equal to, values being compared have to be identical types and values
/*
==
===
!= this is the loose not equal to 
!== this is the strict not equal to 
>
<
>=
<=
*/
console.log(value);
const str = "apple";

console.log(str.charCodeAt(1));
